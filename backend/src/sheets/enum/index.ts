export enum ValidationMessages {
  nameRequired = 'Nome é obrigatório',
  nameMaxLength = 'Nome deve ter no máximo 100 caracteres',
  typeInvalid = 'O tipo da planilha precisa ser válido',
}

export enum SheetType {
  REAL = 'REAL',
  PREVISION = 'PREVISION',
}
